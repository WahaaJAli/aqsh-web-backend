{
  "version": 2,
  "builds": [
    {
      "src": "src/start/server.ts",
      "use": "@vercel/node",
      "config": { "includeFiles": ["src/**/*.ts"] }
    }
  ],
  "routes": [
    { "src": "/(.*)", "dest": "src/start/server.ts" }
  ]
}
